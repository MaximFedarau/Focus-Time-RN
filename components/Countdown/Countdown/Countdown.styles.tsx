//React Native
import { StyleSheet } from 'react-native';

//Constants
import { WHITE, LIGHT_CORNFLOWER_BLUE } from '@constants/colors';

export const styles = StyleSheet.create({
  text: {
    fontSize: 80,
    fontWeight: 'bold',
    color: WHITE,
    padding: 24,
    backgroundColor: LIGHT_CORNFLOWER_BLUE,
  },
});
